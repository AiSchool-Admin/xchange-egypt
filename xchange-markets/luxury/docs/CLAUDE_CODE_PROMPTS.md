# 🤖 دليل التطوير مع Claude Code

## Xchange Luxury Marketplace Development Guide

---

## 📋 نظرة عامة

هذا الدليل يحتوي على prompts جاهزة للاستخدام مع Claude Code لبناء منصة Xchange للسلع الفاخرة.

---

## 🚀 Phase 1: إعداد المشروع

### Prompt 1.1: إنشاء هيكل المشروع

```
أنشئ مشروع Next.js 14 مع TypeScript لمنصة Xchange Luxury - سوق السلع الفاخرة المستعملة.

المتطلبات:
1. استخدم App Router
2. هيكل monorepo:
   - apps/web (Next.js frontend)
   - apps/api (Express backend)
   - packages/shared (types, utils)
   - packages/ui (shared components)

3. التقنيات:
   - Next.js 14 + TypeScript
   - Tailwind CSS
   - Prisma ORM
   - Express.js
   - PostgreSQL

4. إعداد:
   - ESLint + Prettier
   - Husky pre-commit hooks
   - Docker compose للـ DB

5. دعم RTL والعربية من البداية
```

### Prompt 1.2: إعداد قاعدة البيانات

```
باستخدام الـ schema.prisma المرفق، قم بـ:

1. إعداد Prisma مع PostgreSQL
2. تشغيل migrations
3. إضافة البيانات الأولية (seed)
4. إنشاء Prisma Client

تأكد من:
- إضافة جميع indexes المطلوبة
- إعداد relations بشكل صحيح
- إضافة triggers للـ updated_at
```

---

## 🔐 Phase 2: نظام المصادقة

### Prompt 2.1: OTP Authentication

```
أنشئ نظام مصادقة OTP كامل للتطبيق:

Backend (Express):
1. POST /api/auth/send-otp
   - إرسال OTP للهاتف
   - صلاحية 2 دقائق
   - حد 5 محاولات في الساعة
   
2. POST /api/auth/verify-otp
   - التحقق من OTP
   - إنشاء access token (1 ساعة)
   - إنشاء refresh token (30 يوم)
   - إرجاع بيانات المستخدم

3. POST /api/auth/refresh
   - تجديد access token

4. POST /api/auth/logout
   - إلغاء refresh token

Frontend (Next.js):
1. صفحة /auth/login
   - إدخال رقم الهاتف
   - إرسال OTP
   - إدخال OTP
   - redirect للصفحة الرئيسية

2. AuthContext للتحكم بالحالة
3. Middleware لحماية الصفحات
4. useAuth hook

استخدم:
- JWT للتوكنات
- bcrypt للـ OTP
- Zustand للـ state
- React Hook Form للـ forms
- Zod للـ validation
```

### Prompt 2.2: حماية API

```
أنشئ middleware للتحقق من التوكنات:

1. authMiddleware
   - التحقق من Bearer token
   - فك تشفير JWT
   - إضافة user للـ request

2. requireRole middleware
   - التحقق من صلاحيات المستخدم
   - user, expert, admin

3. rateLimiter
   - حد 100 طلب/دقيقة للمستخدمين
   - حد 1000 طلب/دقيقة للـ API keys

4. Error handling middleware
   - أكواد خطأ موحدة
   - رسائل بالعربية والإنجليزية
```

---

## 📱 Phase 3: واجهة المستخدم الأساسية

### Prompt 3.1: Layout والتنقل

```
أنشئ Layout أساسي للتطبيق:

1. Header:
   - Logo
   - بحث
   - إشعارات (bell icon)
   - حساب المستخدم
   - سلة المفضلات

2. Bottom Navigation (موبايل):
   - الرئيسية
   - البحث
   - إضافة منتج (+)
   - الرسائل
   - حسابي

3. Sidebar (ديسكتوب):
   - فئات المنتجات
   - فلاتر سريعة
   - ماركات مميزة

متطلبات:
- RTL كامل
- خط Cairo للعربية
- ألوان:
  - Primary: #1a1a2e (كحلي غامق)
  - Secondary: #c9a227 (ذهبي)
  - Accent: #e94560 (أحمر)
- Responsive design
- Dark mode ready
```

### Prompt 3.2: الصفحة الرئيسية

```
أنشئ الصفحة الرئيسية للتطبيق:

1. Hero Section:
   - بانر ترويجي
   - بحث بارز
   - فئات سريعة

2. Featured Products:
   - Carousel للمنتجات المميزة
   - عرض 4-6 منتجات

3. Categories Grid:
   - أيقونات الفئات
   - عدد المنتجات في كل فئة

4. Recently Listed:
   - أحدث المنتجات
   - Infinite scroll

5. Top Sellers:
   - البائعين المميزين
   - تقييماتهم

6. Price Trends (اختياري):
   - رسم بياني لاتجاهات الأسعار
   - أكثر الموديلات طلباً

استخدم:
- Server Components للبيانات الثابتة
- Client Components للتفاعلية
- Suspense للـ loading
- Image optimization
```

---

## 🛍️ Phase 4: المنتجات

### Prompt 4.1: قائمة المنتجات

```
أنشئ صفحة عرض المنتجات /products:

1. فلاتر متقدمة:
   - الفئة
   - الماركة (متعددة)
   - السعر (من - إلى)
   - الحالة
   - حالة التحقق
   - الموقع

2. خيارات العرض:
   - Grid / List view
   - ترتيب (الأحدث، السعر، الشعبية)
   - عدد النتائج

3. بطاقة المنتج:
   - صورة رئيسية
   - الماركة والموديل
   - السعر
   - الحالة
   - شارة التحقق
   - زر المفضلة

4. Pagination / Infinite scroll

5. حالات فارغة:
   - لا توجد نتائج
   - خطأ في التحميل

API Endpoints:
- GET /api/products (مع query params للفلاتر)
- استخدم React Query للـ caching
```

### Prompt 4.2: صفحة تفاصيل المنتج

```
أنشئ صفحة تفاصيل المنتج /products/[id]:

1. معرض الصور:
   - صور متعددة
   - Zoom on hover
   - Fullscreen gallery
   - صور التفاصيل والسيريال

2. معلومات المنتج:
   - العنوان والوصف
   - الماركة والموديل
   - السعر (مع مقارنة بالجديد)
   - الحالة (مع توضيح)
   
3. شهادة التحقق:
   - شارة Xchange Verified
   - تفاصيل التحقق (AI / Expert)
   - رابط الشهادة

4. تفاصيل خاصة:
   - للساعات: الحركة، الحجم، المواد، السنة
   - للحقائب: المقاس، المواد، اللون
   - للمجوهرات: المعدن، الأحجار، الشهادة

5. معلومات البائع:
   - الاسم والصورة
   - مستوى البائع
   - التقييم
   - عدد المبيعات
   - تاريخ الانضمام

6. أزرار التفاعل:
   - شراء الآن
   - تقديم عرض
   - طلب مقايضة
   - إضافة للمفضلة
   - مشاركة

7. منتجات مشابهة

API:
- GET /api/products/:id
- POST /api/products/:id/view (لزيادة العداد)
```

### Prompt 4.3: إضافة منتج جديد

```
أنشئ صفحة إضافة منتج /sell/new:

1. Stepper (4 خطوات):
   
   الخطوة 1 - اختيار الفئة:
   - اختيار الفئة الرئيسية
   - اختيار الماركة
   - اختيار الموديل (اختياري)

   الخطوة 2 - الصور:
   - رفع 5-15 صورة
   - drag & drop
   - توجيهات لكل صورة مطلوبة
   - image compression

   الخطوة 3 - التفاصيل:
   - نموذج ديناميكي حسب الفئة
   - للساعات: حركة، حجم، مواد، سنة، علبة/أوراق
   - للحقائب: مقاس، مادة، لون، date code
   - الحالة + ملاحظات
   - الوصف

   الخطوة 4 - التسعير:
   - السعر المطلوب
   - اقتراح سعر AI
   - قبول التفاوض؟
   - قبول المقايضة؟
   - اختيار طريقة التحقق

2. Preview قبل النشر

3. حفظ كمسودة

API:
- POST /api/products (create)
- PUT /api/products/:id (update)
- POST /api/products/:id/images (upload)
- POST /api/products/:id/publish
```

---

## 💰 Phase 5: العروض والمعاملات

### Prompt 5.1: نظام العروض

```
أنشئ نظام العروض:

1. صفحة تقديم عرض:
   - عرض نقدي (إدخال المبلغ)
   - عرض مقايضة (اختيار منتج)
   - عرض مختلط
   - رسالة للبائع
   - صلاحية العرض

2. صفحة العروض المستلمة:
   - قائمة العروض على منتجاتي
   - فلتر حسب المنتج/الحالة
   - قبول/رفض/عرض مضاد
   - مؤقت للصلاحية

3. صفحة العروض المرسلة:
   - قائمة عروضي
   - حالة كل عرض
   - سحب العرض

4. Real-time updates:
   - WebSocket للإشعارات
   - تحديث الحالة مباشرة

API:
- GET /api/offers/received
- GET /api/offers/sent
- POST /api/offers
- PUT /api/offers/:id/accept
- PUT /api/offers/:id/reject
- DELETE /api/offers/:id (withdraw)
```

### Prompt 5.2: نظام الدفع Escrow

```
أنشئ نظام الدفع مع Escrow:

1. صفحة الدفع /checkout/:transactionId:
   - ملخص المعاملة
   - اختيار طريقة الدفع
   - إدخال بيانات الدفع
   - تأكيد الدفع

2. تكامل Paymob:
   - إنشاء payment intent
   - iframe للدفع
   - webhook للتأكيد
   - حفظ المبلغ في escrow

3. صفحة تتبع المعاملة:
   - Timeline الحالة:
     - تم الدفع ✓
     - تم الشحن
     - تم التوصيل
     - فترة الفحص (14 يوم)
     - مكتمل
   - معلومات الشحن
   - أزرار التفاعل

4. تأكيد الاستلام:
   - المشتري يؤكد
   - أو auto-release بعد 14 يوم
   - فتح نزاع

API:
- POST /api/transactions/:id/pay
- POST /api/transactions/:id/ship
- POST /api/transactions/:id/confirm
- POST /api/transactions/:id/dispute

Webhooks:
- POST /webhooks/paymob
```

---

## 🔍 Phase 6: التحقق من الأصالة

### Prompt 6.1: واجهة التحقق

```
أنشئ نظام عرض التحقق:

1. شارات التحقق:
   - AI Verified (أزرق)
   - Expert Verified (ذهبي)
   - Fully Verified (أخضر + ذهبي)

2. صفحة شهادة التحقق /verify/:productId:
   - معلومات المنتج
   - نتيجة فحص AI
   - نتيجة فحص الخبير
   - نقاط الفحص
   - تاريخ التحقق
   - QR code للتحقق

3. طلب تحقق إضافي:
   - طلب فحص خبير
   - جدولة موعد
   - تتبع الطلب

4. للبائعين:
   - خيارات التحقق عند الإضافة
   - تكلفة كل مستوى
   - وقت الانتظار

API:
- GET /api/authentication/:productId
- POST /api/authentication/request
- GET /api/authentication/status/:requestId
```

### Prompt 6.2: تكامل Entrupy (للحقائب)

```
أنشئ تكامل مع Entrupy API:

1. Service class:
   - authenticate(product) → result
   - getStatus(requestId) → status
   - getCertificate(productId) → url

2. Workflow:
   - البائع يرفع صور محددة
   - إرسال للـ API
   - انتظار النتيجة
   - تحديث المنتج
   - إشعار البائع

3. Error handling:
   - صور غير واضحة
   - ماركة غير مدعومة
   - timeout

4. Mock للتطوير:
   - محاكاة الـ API
   - نتائج تجريبية
```

---

## 📊 Phase 7: لوحة التحكم

### Prompt 7.1: Dashboard المستخدم

```
أنشئ لوحة تحكم المستخدم /dashboard:

1. نظرة عامة:
   - إحصائيات سريعة
   - إجمالي المبيعات/المشتريات
   - الرصيد المتاح
   - منتجات نشطة

2. منتجاتي:
   - قائمة المنتجات
   - فلتر حسب الحالة
   - تعديل/حذف/تجديد

3. معاملاتي:
   - المبيعات
   - المشتريات
   - فلتر حسب الحالة

4. المحفظة:
   - الرصيد
   - تاريخ المعاملات
   - طلب سحب

5. الإعدادات:
   - الملف الشخصي
   - التحقق من الهوية
   - الإشعارات
   - تغيير اللغة
```

### Prompt 7.2: Admin Dashboard

```
أنشئ لوحة تحكم المدير /admin:

1. نظرة عامة:
   - GMV اليومي/الأسبوعي/الشهري
   - عدد المعاملات
   - عدد المستخدمين الجدد
   - نسبة التحويل

2. إدارة المنتجات:
   - قائمة بانتظار المراجعة
   - رفض/قبول
   - تعديل

3. إدارة المستخدمين:
   - قائمة المستخدمين
   - ترقية/تخفيض المستوى
   - حظر

4. إدارة النزاعات:
   - النزاعات المفتوحة
   - التحكيم
   - الاسترداد

5. التقارير:
   - تقارير المبيعات
   - تقارير المستخدمين
   - export to CSV/PDF

6. الإعدادات:
   - العمولات
   - الماركات والفئات
   - إعدادات التحقق
```

---

## 📱 Phase 8: تطبيق الموبايل

### Prompt 8.1: React Native App

```
أنشئ تطبيق React Native:

1. إعداد المشروع:
   - Expo أو React Native CLI
   - React Navigation
   - مشاركة الـ types مع الويب

2. الشاشات الأساسية:
   - Splash screen
   - Auth flow
   - Home
   - Search
   - Product details
   - Add product
   - Profile

3. ميزات خاصة:
   - Push notifications
   - Camera للصور
   - Deep linking
   - Offline support

4. UI Components:
   - إعادة استخدام من packages/ui
   - تصميم متوافق مع الويب
```

---

## 🔧 إعدادات إضافية

### Environment Variables (.env.example)

```env
# Database
DATABASE_URL="postgresql://user:pass@localhost:5432/xchange_luxury"

# Redis
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="your-super-secret-key"
JWT_EXPIRES_IN="1h"
REFRESH_TOKEN_EXPIRES_IN="30d"

# SMS Provider
SMS_PROVIDER="vodafone" # or twilio
VODAFONE_API_KEY=""
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""

# Payment
PAYMOB_API_KEY=""
PAYMOB_INTEGRATION_ID=""
PAYMOB_IFRAME_ID=""
PAYMOB_HMAC_SECRET=""

# Storage
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_S3_BUCKET=""
AWS_REGION="eu-west-1"

# Or Cloudinary
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Authentication
ENTRUPY_API_KEY=""
ENTRUPY_API_URL=""

# Firebase (Push Notifications)
FIREBASE_PROJECT_ID=""
FIREBASE_PRIVATE_KEY=""
FIREBASE_CLIENT_EMAIL=""

# App
NEXT_PUBLIC_API_URL="http://localhost:3001"
NEXT_PUBLIC_WS_URL="ws://localhost:3001"
```

---

## 📅 خطة التنفيذ المقترحة

| الأسبوع | المهام |
|---------|--------|
| 1-2 | Phase 1 + 2 (Setup + Auth) |
| 3-4 | Phase 3 (UI الأساسية) |
| 5-6 | Phase 4 (المنتجات) |
| 7-8 | Phase 5 (العروض والدفع) |
| 9-10 | Phase 6 (التحقق) |
| 11-12 | Phase 7 (Dashboards) |
| 13-14 | Phase 8 (Mobile) + Testing |

---

## 🎯 أولويات MVP

**يجب تنفيذها أولاً:**
1. ✅ Authentication
2. ✅ عرض المنتجات
3. ✅ إضافة منتج
4. ✅ نظام العروض البسيط
5. ✅ الدفع Escrow

**يمكن تأجيلها:**
- المقايضة المعقدة
- تكامل Entrupy
- تطبيق الموبايل
- Admin Dashboard المتقدم

---

*آخر تحديث: ديسمبر 2024*
