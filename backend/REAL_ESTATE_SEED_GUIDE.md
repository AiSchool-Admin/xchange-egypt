# دليل إضافة البيانات التجريبية لسوق العقارات
# Real Estate Seed Data Guide

## 🎯 الهدف
إضافة 8 عقارات تجريبية متنوعة لاختبار ميزات سوق العقارات المتقدمة.

---

## 📦 البيانات التجريبية المتضمنة

### أنواع العقارات:
1. ✅ شقة فاخرة في التجمع الخامس (Apartment)
2. ✅ فيلا في الشيخ زايد (Villa)
3. ✅ بنتهاوس في المعادي (Penthouse)
4. ✅ ستوديو في 6 أكتوبر (Studio)
5. ✅ دوبلكس في مصر الجديدة (Duplex)
6. ✅ شاليه في الساحل الشمالي (Chalet)
7. ✅ تاون هاوس في الرحاب (Townhouse)
8. ✅ مكتب في وسط البلد (Office)

### المميزات:
- ✅ أسعار متنوعة (950,000 - 12,000,000 جنيه)
- ✅ مساحات مختلفة (55 - 450 متر)
- ✅ مواقع متعددة (القاهرة، الجيزة، الساحل الشمالي)
- ✅ مستويات تشطيب مختلفة
- ✅ عدادات Views & Favorites لاختبار التوصيات

---

## 🚀 طرق التشغيل

### الطريقة 1: على Railway (الإنتاج) ✅ موصى بها

1. افتح **Railway Dashboard**
2. اذهب إلى مشروعك → **Settings** → **Variables**
3. تأكد من وجود `DATABASE_URL`
4. اذهب إلى **Deployments**
5. افتح **Console/Terminal**
6. نفذ الأمر التالي:

```bash
cd backend && npm run seed:real-estate
```

أو مباشرة:
```bash
cd backend && npx tsx scripts/seed-real-estate-quick.ts
```

### الطريقة 2: محلياً (إذا كانت Prisma تعمل)

```bash
cd backend
npm run seed:real-estate
```

### الطريقة 3: عبر Prisma Studio

```bash
cd backend
npx prisma studio
# ثم أضف البيانات يدوياً من الواجهة
```

---

## ✅ التحقق من النجاح

بعد تشغيل Seed، يجب أن ترى:

```
✅ Using user: Xchange Admin (admin@xchange.eg)

Creating sample properties...
  ✓ شقة فاخرة في التجمع الخامس
  ✓ فيلا في الشيخ زايد
  ✓ بنتهاوس في المعادي
  ✓ ستوديو في 6 أكتوبر
  ✓ دوبلكس في مصر الجديدة
  ✓ شاليه في الساحل الشمالي
  ✓ تاون هاوس في الرحاب
  ✓ مكتب في وسط البلد

✅ Created 8/8 properties

🎉 Seed completed successfully!
```

---

## 🧪 اختبار الميزات

### 1. الصفحة الرئيسية `/real-estate`
- ✅ يجب أن ترى 8 عقارات
- ✅ جرب الفلاتر (النوع، السعر، المحافظة)
- ✅ جرب الترتيب (السعر، المساحة)

### 2. التقييم الآلي `/real-estate/valuation`
- ✅ أدخل بيانات عقار مشابه (مثلاً شقة 180م في التجمع الخامس)
- ✅ يجب أن تحصل على تقييم بناءً على البيانات الموجودة

### 3. التوصيات `/real-estate/recommendations`
- ✅ يجب أن ترى توصيات Trending (العقارات الأكثر مشاهدة)
- ✅ شاهد بعض العقارات ثم عد - يجب أن تتغير التوصيات

### 4. المقايضة `/real-estate/barter`
- ✅ حاول إيجاد مطابقات بين العقارات الموجودة

---

## 🔄 إضافة المزيد من البيانات

إذا أردت **إضافة المزيد من العقارات**:

### خيار 1: تشغيل Seed الكامل
```bash
cd backend
npx tsx prisma/seeds/seed-real-estate.ts
```
سيضيف **50+ عقار** مع بيانات شاملة!

### خيار 2: استخدام SQL Seed
```bash
# على Railway Console
psql $DATABASE_URL < prisma/seeds/properties_comprehensive_seed.sql
```

---

## 🛠️ استكشاف الأخطاء

### خطأ: "User not found"
**الحل**: الـ Script سينشئ مستخدم admin@xchange.eg تلقائياً

### خطأ: "Prisma Client not found"
**الحل**:
```bash
npx prisma generate
```

### خطأ: "Database connection failed"
**الحل**: تأكد من `DATABASE_URL` في `.env`

---

## 📊 إحصائيات البيانات

| النوع | العدد | نطاق السعر |
|------|-------|------------|
| Apartments | 2 | 950k - 4.5M |
| Villas | 1 | 12M |
| Penthouses | 1 | 8.5M |
| Others | 4 | متنوع |

**إجمالي القيمة**: ~42.15 مليون جنيه
**متوسط السعر**: ~5.27 مليون جنيه

---

## 🎓 للمطورين

### إضافة عقارات مخصصة

عدل الملف `scripts/seed-real-estate-quick.ts`:

```typescript
const SAMPLE_PROPERTIES = [
  // ... العقارات الموجودة
  {
    title: 'Your Property Title',
    titleAr: 'عنوان العقار بالعربي',
    propertyType: PropertyType.APARTMENT,
    governorate: 'Cairo',
    // ... بقية البيانات
  },
];
```

ثم شغّل:
```bash
npm run seed:real-estate
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من logs في Railway Console
2. تأكد من Prisma schema محدث
3. تحقق من DATABASE_URL

---

**آخر تحديث**: ديسمبر 2025
**الإصدار**: 1.0.0
